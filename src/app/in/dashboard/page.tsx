import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard / IoTouch",
};

export default function Dashboard() {
  return (
    <>
      <div>Dashboard</div>
    </>
  );
}
